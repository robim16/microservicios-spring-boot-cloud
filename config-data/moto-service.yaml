server:
  port: 8003
eureka:
  client:
    fecth-registry: false
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
    instance:
     hostname: localhost